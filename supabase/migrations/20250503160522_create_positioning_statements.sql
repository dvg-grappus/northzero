CREATE TABLE IF NOT EXISTS positioning_statements ( id uuid PRIMARY KEY DEFAULT gen_random_uuid(), project_id uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE, statements_json jsonb NOT NULL, created_at timestamptz DEFAULT now(), created_by uuid REFERENCES auth.users(id) ); CREATE UNIQUE INDEX IF NOT EXISTS positioning_statements_project_id_idx ON positioning_statements(project_id);
